---
import Layout from '../../layouts/Layout.astro';
import LessonPage from '../../components/pages/lessons/LessonPage';
import { LESSONS } from '../../utils/curriculum/lessons';

export const prerender = true;

export function getStaticPaths() {
  return LESSONS.map((lesson) => ({ params: { id: lesson.id } }));
}

const { id } = Astro.params;
---

<Layout title="Typing Dvorak — Lesson">
  <LessonPage client:load lessonId={id!} />
</Layout>
