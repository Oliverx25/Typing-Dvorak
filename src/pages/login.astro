---
import Layout from '@/layouts/Layout.astro';
import LoginForm from '@/components/auth/forms/LoginForm';

export const prerender = true;
---

<Layout title="Typing Dvorak — Sign in">
  <LoginForm client:only="react" />
</Layout>
