import AppChrome from '@/components/layout/shell/AppChrome';
import HistoryPage from '@/components/pages/history/HistoryPage';

export default function HistoryAppRoot() {
  return (
    <AppChrome>
      <HistoryPage />
    </AppChrome>
  );
}
